@using ValidationPoc.Models
@model ValidationPoc.Models.OtherGreens?
@{

    var values = Enum.GetValues(typeof(OtherGreens)).Cast<object>()
                     .Select(v => new SelectListItem
                     {
                         Selected = v.Equals(Model),
                         Text = v.ToString(),
                         Value = v.ToString()
                     });
}

@Html.DropDownList("", values, "Please Choose", new { data_ng_model = Html.NameForModel()})